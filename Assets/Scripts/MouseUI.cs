using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MouseUI : MonoBehaviour
{
/*    public Texture NormalBlock;*/
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
/*    public void OnGUI()
    {
        Vector3 mousePosition = Input.mousePosition;
        GUI.DrawTexture(new Rect(mousePosition.x - NormalBlock.width / 2, Screen.height - mousePosition.y - NormalBlock.height / 2, NormalBlock.width, NormalBlock.height), NormalBlock);
    }*/
}
